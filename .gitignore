# Build artefacts
dist/
build_site/
site/
outputs/
.cache/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenvs (never used — Docker only — but guard anyway)
.venv/
venv/
env/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Local secrets / env
.env
.env.*
!.env.example

# Streamlit
.streamlit/secrets.toml

# Large raw data files (regenerable from documented URLs in data_sources/SOURCES.md)
data_sources/inglehart_welzel/wvs_time_series_1981_2022.csv
data_sources/vdem/vdem.RData
data_sources/undp/HDR25_Composite_indices_complete_time_series.csv

# GeoEPR raw shapefile ZIP + extracted shapefile directory — auto-downloaded
# by apps/basis_builder/load_geoepr.py. Only the normalised JSON sidecar
# (ethnic_records_normalised.json) and the small EPR-Core CSV are committed.
data_sources/geoepr/GeoEPR-2021.zip
data_sources/geoepr/GeoEPR-2021/

# Glottolog raw CSV — auto-downloaded by apps/basis_builder/load_glottolog.py.
# Only the normalised JSON sidecar (glottolog_languoids_normalised.json) is
# committed.
data_sources/glottolog/languages.csv
